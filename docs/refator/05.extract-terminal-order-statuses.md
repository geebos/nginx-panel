# 05. Extract terminal ACME order statuses constant

## Reflection (iteration 5)

### Accomplished

Four prior pure extracts landed with full design-challenger → TDD → code-reviewer loop.

### Working well

- Pure helpers with exact behavior preservation
- Small diffs, high test confidence
- Prefer extract over large rewrites

### Adjustments

- Prefer higher-value drifts next (e.g. active vs terminal order status source of truth with renewal)
- Avoid formatting-only noise; restore if tools reformat unrelated files

### This iteration

Share `terminalOrderStatuses` between domain and manager certificate modules (approved as small but real duplication).

## 问题

`["succeeded","failed","expired","cancelled"]` 在 routes 与 manager certificate 重复。

## 方案

`src/worker/lib/acme/order-status.ts` 导出 `string[]` 常量（避免 `as const` 与 Drizzle/includes 摩擦）。

## Design Challenger

APPROVE；可选 skip；更高价值项是与 renewal active 列表统一（留待后续）。

## Code Reviewer

**APPROVED** — 无 BLOCKING。NON_BLOCKING：后续可考虑与 renewal 的 active 列表统一 source of truth。

## 验证

- order-status + certificates tests pass
- typecheck clean
