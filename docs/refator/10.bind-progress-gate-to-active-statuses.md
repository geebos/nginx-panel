# 10. Bind ACME progress gate to activeOrderStatuses

## Reflection (iteration 10)

### Accomplished (1–9)

Pure extracts: hostnames, ACME public serializers, master-key, safe-error, order status constants (terminal shared FE+worker, active, recheckable), hostname mutability wiring.

### Working well

- design-challenger → TDD → code-reviewer
- Prefer status-set drift fixes over micro-DRY of object shapes
- Surgical patches; restore formatter noise immediately

### Adjustments

- Design challenger correctly REVISE'd "second literal list" proposals
- Prefer derive/inline predicates over parallel arrays

### Next priorities

- RECHECK_DEBOUNCE_MS (two recheck sites)
- Nested ternary / runner pure splits when high value
- Avoid touching large UI files with auto-format on

## 问题

`progressAcmeOrder` 硬编码 progress 五态，与 `activeOrderStatuses` 去掉 `preparing` 同义，存在调度选中 vs 门闩拒绝的漂移风险。

## Design Challenger

**REVISE**：禁止第二份手写列表；优先内联  
`order.status === "preparing" || !activeOrderStatuses.includes(order.status)`。

## 方案

按 REVISE 实现内联谓词；测试钉住 active − preparing 不变量。

## Code Reviewer

**APPROVED** — 无 BLOCKING。

## 验证

- order-status + scheduler tests → 16 pass
- typecheck / lint clean
