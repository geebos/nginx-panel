# 11. Extract RECHECK_DEBOUNCE_MS

## 问题

domain 与 manager manual recheck 均硬编码 `5_000` 毫秒 debounce（基于 `lastPolledAt`）。

## 方案

在 `order-status.ts` 导出 `RECHECK_DEBOUNCE_MS`，两处引用。注释标明字段是 `lastPolledAt`。不与 ACME scheduler `setInterval(5_000)` 共用。

## Design Challenger

**APPROVE**

## Code Reviewer

**APPROVED** — 无 BLOCKING。

## 验证

- order-status + certificates tests → 16 pass
- typecheck / lint clean
