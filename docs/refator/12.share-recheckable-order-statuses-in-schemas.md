# 12. Share recheckableOrderStatuses via shared schemas

## 问题

domain/manager SSL UI 用字面量三态控制 recheck 按钮，与 worker `recheckableOrderStatuses` 同集合，存在 UI/API 策略漂移。

## 方案

- `shared/schemas/certificate.ts` 导出 `recheckableOrderStatuses`
- worker `order-status.ts` re-export（与 terminal 同模式）
- 两处 SSL 页改为 `@/shared/schemas` import
- `activeOrderStatuses` 仍仅 worker

## Design Challenger

**APPROVE**

## Code Reviewer

**APPROVED** — 无 BLOCKING。

## 验证

- order-status + certificates tests → 17 pass
- typecheck / lint clean
