# 13. Extract downloadableOrderStatuses

## 问题

`scheduler.ts` 两处硬编码 `["validated", "downloading"]`（progress 分支 + download 完成 gate），与 order-status 策略面脱节。

## 方案

worker-only 导出 `downloadableOrderStatuses`，scheduler 两处引用。不进 shared（无 FE）。不 derive、不抽 helper。

## Design Challenger

**APPROVE**

## Code Reviewer

**APPROVED** — 无 BLOCKING。

## 验证

- order-status + scheduler tests → 21 pass
- typecheck / lint clean
