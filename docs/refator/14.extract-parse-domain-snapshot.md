# 14. Extract parseDomainSnapshot

## 问题

manager 已有 `parseManagerSnapshot`，domain 仍在约 11 处 production 手写 `domainConfigSchema.parse(JSON.parse(...))`；`versions.ts` 还有本地 `configFrom`。

## 方案

- 新增 `src/worker/lib/domain/snapshot.ts`：`parseDomainSnapshot`
- 替换全部 production 调用点；删除 `configFrom`
- 不改 manager helper / auth safeParse / identifiersJson / sansJson
- 不改 `domainConfigSchema.parse({ ...object })` 构造路径

## Design Challenger

**APPROVE**（placement: `domain/snapshot.ts`）

## Code Reviewer

**APPROVED** — 无 BLOCKING。

## 验证

- snapshot + certificates tests → 11 pass
- typecheck / lint clean
