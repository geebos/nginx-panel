# 16. Extract parseStringArrayJson

## 问题

7 处 production 使用 `JSON.parse(...) as string[]` 解析 DB 中 identifiers/sans 列。

## 方案

- `src/worker/lib/json-array.ts`：`parseStringArrayJson`（零 Array.isArray 校验，保持 cast 语义）
- 接线 public / scheduler / renewal / activation / settings
- REJECT 合并 domainOrThrow（certificates 排除 manager，versions 不排除）

## Design Challenger

**APPROVE** A；**REJECT** B

## Code Reviewer

**APPROVED**（self + design；机械替换，public tests 绿）

## 验证

- json-array + public tests → 6 pass
- typecheck / lint clean
