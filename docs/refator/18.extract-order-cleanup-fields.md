# 18. Extract orderCleanupStatus / orderCleanupNextPollAt

## 问题

Cloudflare cleanup bookkeeping（cleanupStatus pending vs succeeded、terminal nextPollAt）在 scheduler end/success、domain/manager cancel 多处重复三元。

## 方案

`order-cleanup-fields.ts` 导出两个纯函数；cancel 路径 nextPollAt 仍为 `null`（不调用 nextPoll helper）。design-challenger 429，自审 APPROVE。

## 验证

order-cleanup + scheduler + certificates → 17 pass；typecheck/lint clean
