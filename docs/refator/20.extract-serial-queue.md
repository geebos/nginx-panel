# 20. Extract createSerialQueue

## 问题

ACME scheduler 与 renewal 各自维护几乎相同的 promise serial tail（catch 日志吞错）。

## 方案

`createSerialQueue(logMessage)` → `{ enqueue, wait }`；acme / renewal 各一实例，log 字面量不变。

## Design Challenger

**APPROVE** A；REJECT B/C/D

## 验证

serial-queue + scheduler + renewal → 15 pass；typecheck clean
