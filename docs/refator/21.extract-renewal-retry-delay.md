# 21. Extract renewalRetryDelayMs

## 问题

renewal 失败 backoff ladder 与 clamp 逻辑内联在 `recordRenewalOrderFailure` / `processDueRenewals`。

## 方案

同文件导出 `renewalRetryDelayMs(failedAttemptCount)`；表驱动单测；processDue catch 仍用 attempt 1（step-0）。

## Design Challenger

**APPROVE** A；REJECT B/C

## 验证

renewal tests → 5 pass；typecheck clean
