# 22. Extract domainDisplayStatus

## 问题

FE 多处重复 `enabled ? runtimeStatus : "disabled"` 控制 StatusBadge。

## 方案

`src/lib/domain-status.ts` 纯函数；接线 dashboard / domains 列表与各 domain 子页；不碰 SSL 证书 badge 策略。

## Design Challenger

**APPROVE**

## 验证

domain-status tests → 2 pass；typecheck/lint clean
