# 23. Fix overview SSL status draft → pending

## 问题

Overview HTTPS card 在“已开 SSL 但无 certificateId”时显示 `draft`，而 SSL 页、域名列表 API（`sslStatus`）均用 `pending`。`draft` 与配置版本草稿语义撞车。

## 方案

一行修正：`overview.tsx` 中间分支改为 `pending`。不同时抽 helper（可选后续）。

## Design Challenger

**REVISE** → 最小修复 APPROVE

## 验证

typecheck / lint clean；无残留 `ssl.enabled ? "draft"`
