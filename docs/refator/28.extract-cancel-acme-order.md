# 28. Extract cancelAcmeOrder

## 发现的问题

域名证书 cancel 路由与 `cancelManagerOrder` 几乎逐行重复同一取消策略（terminal 短路、事务更新、renewal 标记、challenge scrub、CF cleanup）。

## 问题证据和影响范围

- `src/worker/routes/certificates.ts` cancel handler
- `src/worker/lib/manager/certificate.ts` `cancelManagerOrder`
- 单独抽 24h 常量已 REJECT；本轮抽完整策略

## 原始设计

两处各自实现完整 cancel 副作用。

## 最小修改方案

`src/worker/lib/acme/cancel-order.ts`：

- `cancelAcmeOrder(db, order)` 共享策略
- 模块私有 `RENEWAL_CANCELLED_RECHECK_MS = 24h`
- 入口仍负责 load / 404 / 域归属

## Design Challenger 结论

**APPROVE**（完整 cancel helper；24h 不进 shared schema）

## TDD 过程

1. helper 单元测：terminal no-op、非 CF scrub、renewal recheck
2. 实现 helper
3. 接线两入口；既有 certificates cancel 集成测回归

## 最终修改

- `src/worker/lib/acme/cancel-order.ts`（new）
- `src/worker/lib/acme/cancel-order.test.ts`（new）
- `src/worker/routes/certificates.ts`
- `src/worker/lib/manager/certificate.ts`

## Code Reviewer 结论

**APPROVED** — no BLOCKING

## 验证结果

- `pnpm exec tsx --test src/worker/lib/acme/cancel-order.test.ts src/worker/routes/certificates.test.ts` → 10 pass
- typecheck / lint clean

## 后续候选优化

- Nested FE ternaries only with multi-site or bug evidence
- Optional CF cancel unit coverage on helper
- Avoid thin pure exports — REJECTed
