# 31. Fix SSL certs card Auto Renew i18n

## 发现的问题

域名 SSL 页证书卡片硬编码英文 `Auto Renew On/Off`，zh-CN 出现英文残片；certificates 列表已用 i18n。

## 问题证据和影响范围

- `src/pages/[locale]/domains/ssl.tsx` certs card meta line
- `public/locales/domains.json` 缺 `ssl.certsCard.autoRenewOn/Off`

## 原始设计

硬编码：`· Auto Renew {autoRenew ? "On" : "Off"}`

## 最小修改方案

- 新增 `domains.ssl.certsCard.autoRenewOn` / `autoRenewOff`（en + zh-CN）
- JSX 改用 `t(...)`

## Design Challenger 结论

**APPROVE**（真实 i18n bug）

## TDD 过程

无行为单测；typecheck/lint；确认无残留 `Auto Renew` 硬编码。

## 最终修改

- `public/locales/domains.json`
- `src/pages/[locale]/domains/ssl.tsx`

## Code Reviewer 结论

**APPROVED**（自审：最小、无无关 reformat）

## 验证结果

- typecheck / lint clean
- `rg "Auto Renew" src/pages` → none
- locales diff +2 lines only

## 后续候选优化

- Nested FE ternaries still REJECT unless thicker logic
- ACME order detail load extract if drift appears
- Reflection at iteration 10
