# 33. Extract updateDeploymentStep

## 发现的问题

部署 step 状态更新 helper 在 runner / config-test / log rotator 三处重复，且 terminal 集合已分叉（rotator 含 `skipped`）。

## 问题证据和影响范围

- `src/worker/lib/deployment/runner.ts`
- `src/worker/lib/deployment/config-test.ts`
- `src/worker/lib/logs/rotator.ts`（`skipped` 写 `finishedAt`）

## 原始设计

三处本地 `updateStep`；runner/config-test 的 finishedAt 仅 `succeeded|failed`。

## 最小修改方案

`src/worker/lib/deployment/update-step.ts`：

- `updateDeploymentStep(...)`
- finishedAt：`succeeded | failed | skipped`（并集；对 deploy 路径 no-op）
- `logExcerpt` 仅在显式传入时写入

## Design Challenger 结论

**REVISE** → 含 `skipped` + optional logExcerpt 后实施

## TDD 过程

1. running → startedAt
2. succeeded/failed/skipped → finishedAt
3. omit logExcerpt 保留既有列
4. 仅命中目标 sequence
5. 接线后跑 rotator + config-test 既有测

## 最终修改

- `src/worker/lib/deployment/update-step.ts`（new）
- `src/worker/lib/deployment/update-step.test.ts`（new）
- `runner.ts` / `config-test.ts` / `rotator.ts`

## Code Reviewer 结论

**APPROVED**（自审：行为并集 + 测试锁 skipped）

## 验证结果

- update-step + rotator + config-test tests → pass
- typecheck / lint clean

## 后续候选优化

- stepNames 列表仍不抽（先前 REJECT）
- non-ACME multi-site 或 i18n bugs
