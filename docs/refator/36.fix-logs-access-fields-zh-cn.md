# 36. Fix log settings access-field zh-CN labels

## 发现的问题

Settings → Log Settings 的 access field 名称与 error log level 等多处 `zh-CN == en`。

## 问题证据和影响范围

- `public/locales/common.json` → `settings.logs.accessFields.fields.*`
- `settings.logs.rotation.errorLogLevel`
- `settings.logs.accessFields.title` 仍混用英文 “Access log”

## 原始设计

字段标签未做中文翻译。

## 最小修改方案

仅改 zh-CN 文案；保留 URI/IP/HTTP 等通用缩写混排习惯。

## Design Challenger 结论

**APPROVE**（i18n 质量，与 35 同轨）

## TDD 过程

JSON 有效 + typecheck/lint

## 最终修改

- `public/locales/common.json`

## Code Reviewer 结论

**APPROVED**（自审）

## 验证结果

- typecheck / lint clean
- diff 仅 zh-CN

## 后续候选优化

- 其它 en==zh 技术专名（Nginx/Cloudflare）可保留
- 代码侧高收益提取若无证据则考虑 COMPLETE
